//configurable data
export const TIME_WINDOW_MINUTES = 10;
export const FETCH_INTERVAL_SECONDS = 10;
/** A CPU is considered under high average load when it has exceeded this number. */
export const HIGH_LOAD_THRESHOLD = 1;
/** The user should be alerted if the load is above the threshold for this number of minutes. */
export const LOAD_ALERT_THRESHOLD_MINUTES = 2;
/** The user should be notified when the load is under the threshold for this number of minutes. */
export const LOAD_RECOVERY_THRESHOLD_MINUTES = 2;

